import AdminNotificationSettings from "@/components/AdminDashboard/settings/AdminNotificationSettings";
import SettingsTop from "@/components/AdminDashboard/settings/SettingsTop";

const Setting = () => {
  return (
    <div>
      <SettingsTop />
      <AdminNotificationSettings />
    </div>
  );
};

export default Setting;
