import DailyActivity from "./DailyActivity";

const Activity = () => {
  return (
    <div className="w-full flex flex-col xl:flex-row gap-6 mb-10 ">
      <DailyActivity />
    </div>
  );
};

export default Activity;
